# Inside jobs.load.steps or similar
- name: Run loader (dry run to verify)
  run: |
    python scripts/load_sp500_index_series.py \
      --db "Stock Data.db" \
      --pe_csv "./sp500_daily_pe_filled.csv" \
      --growth_csv "./sp500_implied_growth.csv" \
      --dry-run

- name: Run loader (write to DB)
  run: |
    python scripts/load_sp500_index_series.py \
      --db "Stock Data.db" \
      --pe_csv "./sp500_daily_pe_filled.csv" \
      --growth_csv "./sp500_implied_growth.csv"
